# Core test runner
pytest>=8.0

# .env file loading
python-dotenv>=1.0

# Lab004: Transformers (Qwen + RoBERTa)
# NOTE: transformers 5.x breaks roberta.py (removed "question-answering" pipeline)
# and causes demo.py to produce repetitive/degraded output with Qwen. Pin to 4.x.
numpy>=1.24
transformers>=4.45,<5
torch>=2.2
